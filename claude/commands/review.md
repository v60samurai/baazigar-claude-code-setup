Review the current codebase changes for: $ARGUMENTS

Check for:
1. Bugs or logic errors (trace through the happy path AND error paths)
2. Missing error handling or edge cases
3. Type safety issues (any types, missing generics, unsafe assertions)
4. Performance concerns (unnecessary re-renders, N+1 queries, missing indexes)
5. Security issues (unsanitized inputs, exposed secrets, missing auth checks)
6. Code that violates the patterns established in CLAUDE.md or elsewhere in the codebase

Be specific. Reference file names and line numbers. Don't mention things that are fine.
