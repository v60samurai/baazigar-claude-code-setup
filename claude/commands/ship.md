Prepare this for shipping: $ARGUMENTS

Checklist:
1. Run the full test suite (pnpm test). Fix any failures.
2. Run linter (pnpm lint). Fix any issues.
3. Run type checker (pnpm tsc --noEmit). Fix any errors.
4. Check for console.log statements that should be removed.
5. Check for hardcoded values that should be env vars.
6. Check for TODO/FIXME comments that need resolution.
7. Check for any `any` types that crept in.
8. Write a clear commit message summarizing all changes.
9. Create the commit (don't push yet).

Show me the final diff and commit message.
