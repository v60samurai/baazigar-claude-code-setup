Refactor the following with zero behavior change: $ARGUMENTS

Rules:
- Run existing tests first to confirm they pass
- Make changes incrementally, committing after each logical step
- Run tests after each change to catch regressions immediately
- If no tests exist, write them first, then refactor
- Explain each refactoring decision briefly in the commit message
